from setuptools import setup

setup(
    name="Werk.IO Importer",
    version="1.0",
    install_requires=[
        "beautifulsoup4",
        "requests",
        "python-daemon",
        "pymongo"
    ]
);
